# runtime/agents

衡策销管 **运行时** Agent 定义（给 `packages/harness` 加载，不是 Cursor 同事）。

| 文件 | name | 界面名 |
|---|---|---|
| `orchestrator.md` | orchestrator | 系统分配 |
| `lead-intake.md` | lead-intake | 建档 |
| `pipeline.md` | pipeline | 商机 |
| `followup.md` | followup | 跟进 |

不要写到 `.cursor/agents`。真跑 Agent 留 M2 `/sdk`。
