# 活动字段

| 字段 | 说明 |
|---|---|
| opportunity_id | 必须指向已转化商机 |
| type | `电话` / `企微` / `邮件` / `拜访` |
| text | 中文话术，无 emoji，无真实手机号 |
| effective | 0 草稿；1 已发出 |

viewer 不能确认。manager 可确认（含 sales 权限）。

跟进只读商机，不推进谈判或赢单。报价/合同首版不做，不要在活动里假装已出合同。
