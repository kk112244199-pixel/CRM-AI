# M4 检查清单（Docker + nginx）

步骤：单机 `docker compose`。无 Redis、无 MQ、无独立 Grafana。`apps/web-demo` 不上生产镜像。

| 项 | 结果 |
|---|---|
| 先读 M3 契约 | 通过 |
| 服务：nginx :80 反代 `/` 与 `/api`；api Node；web 静态 | 通过（compose 测试） |
| volume `hengce-data` 挂 `/data/hengce.db`（`HENGCE_DB`） | 通过 |
| 无 redis / rabbitmq / grafana / prometheus | 通过 |
| web-demo 在 `.dockerignore`，不进镜像 | 通过 |
| `CURSOR_API_KEY` 只进 api 容器 | 通过 |
| API 监听 `0.0.0.0`，供容器互访 | 通过 |
| 无密钥：默认 `HENGCE_RUNNER=scripted`，可回放种子 | 约定（compose 默认） |
| 有密钥：可设 `HENGCE_RUNNER=cursor` 再讲黄金切片 | 约定 |
| `npx tsc --noEmit` | 通过 |
| `npm test` | 56 passed，1 skipped（golden） |
| `docker compose config` | 通过 |
| `docker compose build` / `up` | 本机 Docker 引擎未运行（pipe 不存在），**未假装通过** |

契约：[contracts/M4.md](../contracts/M4.md)。S9 已交付。
