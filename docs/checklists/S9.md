# S9 检查清单（SQLite 向量）

步骤：`searchSimilar` 换成 sqlite-vec。未改漏斗、闸门、八家名字、`web-demo`。无 emoji。

| 项 | 结果 |
|---|---|
| 先读 M6 契约 | 通过 |
| 依赖 `sqlite-vec`，开库加载扩展 | 通过 |
| `vec_leads` 虚拟表；documents 无 embedding 列 | 通过 |
| 检索 SQL 含 MATCH，不含标签 overlap | 通过 |
| Q6 Recall@1：杭齿→嘉兴；邻里鲜→夜灯；海图第一名不是杭齿 | 通过 |
| 未改 harness 闸门 / 四 Agent 顺序 | 通过 |
| 未改 `apps/web-demo` | 通过 |
| 无 Pinecone / Redis / GraphRAG | 通过 |
| `npm test` | 71 passed（不含 golden） |
| `npm --prefix apps/web test` | 4 passed |
| `npm run eval:report` | 72 passed（含黄金切片）；生成物，不手写 |

契约：[contracts/S9.md](../contracts/S9.md)。S10 已交付，见 [contracts/S10.md](../contracts/S10.md)。
