# M2 检查清单（S6 `/sdk` harness 真跑）

步骤：`packages/harness` 按序拉起四个 CRM Agent；`apps/api` 露出顺序 HTTP。未改 `apps/web-demo`。未嵌 `/orchestrate` 的 `plan.json`。

| 项 | 结果 |
|---|---|
| 先读 M1 / M0.5 契约 | 通过 |
| `Agent.create` + `send` + `wait`；显式 `local: { cwd }`；`finally` dispose | 通过（`cursor-runner.ts` + 形状测试） |
| 无 `cloud: { repos }` | 通过 |
| 同一线索顺序交卷；ingest 不并行四个 Agent | 通过（测试） |
| ACL：系统分配不得写三表；建档不得写 stage；跟进不得改 opp.stage；越权 `audit_events` 并拒绝 | 通过 |
| 非法 JSON 不入库；失败 handoff 可见，后续不改库 | 通过 |
| Token 写入 `agent_runs` / `token_ledger`；合计 = 各 run 之和；无充值表 | 通过 |
| HITL：分配/建档自动；转化/外发待确认；赢单/丢单仅 manager | 通过 |
| 无 UI 橙果可到转化待确认，确认后到外发待确认 | 通过（scripted，无密钥） |
| `POST /api/leads/ingest` 顺序跑到转化待确认，确认后再跟进 | 通过 |
| 密钥不进 `apps/web-demo` / 不进 API 响应 | 通过 |
| 无密钥：顺序 / ACL / schema / Token 加法 | 通过 |
| 黄金切片 `npm run test:golden` | 跳过（本机无 `CURSOR_API_KEY`，未假装通过） |
| `npx tsc --noEmit` | 通过 |
| `npm test` | 45 passed，1 skipped（golden） |

契约：[contracts/M2.md](../contracts/M2.md)。当时下一刀 M3。S9 已交付。
