# M6 检查清单（S8 `/frontend-design`）

步骤：只改 `apps/web`。主画面是漏斗交卷轨。未改调度、闸门、`apps/web-demo`。无 emoji。

| 项 | 结果 |
|---|---|
| 先读 M5 契约 | 通过 |
| 只改 `apps/web` | 通过 |
| 未改 harness / API 闸门 / 四 Agent 顺序 | 通过 |
| 未改 `apps/web-demo` | 通过 |
| 主画面：系统分配 / 建档 / 转化 / 跟进 漏斗轨 | 通过 |
| 待确认印泥章；转化/外发/赢单闸门文案仍在 | 通过 |
| 无通用紫渐变、无 KPI dashboard 模板、无 emoji | 通过（样式测试） |
| 五屏仍在：列表、详情、待确认、用量、录入 | 通过 |
| R6 前端无密钥 | 通过 |
| `npm --prefix apps/web test` | 4 passed |
| `npm --prefix apps/web run build` | 通过 |
| `npm test` | 64 passed，1 skipped（golden） |

契约：[contracts/M6.md](../contracts/M6.md)。S9、S10 已交付。
