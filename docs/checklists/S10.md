# S10 检查清单（线索筛选与 CSV 导入）

步骤：`GET /api/leads?status=&stage=` 服务端筛选；`POST /api/leads/import` CSV 批量导入；`apps/web` 阶段 chips 与导入工具栏。未改漏斗、闸门、八家名字、`web-demo`。无 emoji。

| 项 | 结果 |
|---|---|
| 先读 S9 契约 | 通过 |
| `GET /api/leads?status=&stage=` 合法枚举过滤；未知值 400 | 通过 |
| `status=新线索` 含橙果素质教育 | 通过 |
| `stage=方案报价` 含杭齿精密机电、邻里鲜超市 | 通过 |
| `stage=丢单` 含江东水务物资 | 通过 |
| `POST /api/leads/import`：`sales`/`manager` 允许；`viewer` 403 | 通过 |
| CSV 导入不触发 `ingestLead` 或 Agent；重复公司跳过 | 通过 |
| 成功插入后 `rebuildLeadVec` | 通过 |
| `apps/web` 保留 tabs；新增 stage chips 与 CSV 导入 | 通过 |
| 未改 harness 闸门 / 四 Agent 顺序 | 通过 |
| 未改 `apps/web-demo` | 通过 |
| 无 Pinecone / Redis / GraphRAG | 通过 |
| `npm test` | 82 passed（不含 golden） |
| `npm --prefix apps/web test` | 4 passed |
| `npm run eval:report` | 未在本步重跑；沿用 S9 报告 |

契约：[contracts/S10.md](../contracts/S10.md)。S10 已交付。
