# M5 检查清单（eval 黄金切片与报告）

步骤：无独立 skill。按 [eval.md](../eval.md) 跑 Q1–Q8、R1–R6。报告由 `npm run eval:report` 生成，不手写假装通过。

| 项 | 结果 |
|---|---|
| 先读 M4 契约与 eval.md | 通过 |
| Q1 脚本切片（橙果顺序 handoff） | 通过（见 eval-last-run） |
| Q1 真模型黄金切片 | 跳过（本机无 `CURSOR_API_KEY`，未假装通过） |
| Q2 职责隔离 | 通过 |
| Q3 非法 JSON 不得入库 | 通过 |
| Q4 转化/外发闸门；赢单 manager；中间阶段销售自推 | 通过 |
| Q5 语料仅 8 家 | 通过 |
| Q6 杭齿→嘉兴精工装备；邻里鲜→夜灯便利；海图第一名不是杭齿 | 通过 |
| Q7 中文状态/阶段，无 Discovery | 通过 |
| Q8 引用对照 corpus 文件名 | 通过（不评向量 Recall） |
| R1 pipeline 前杀掉再续跑 | 通过 |
| R2 Token 合计 = runs = ledger | 通过 |
| R3 失败 handoff 可见，后续不改库 | 通过 |
| R4 orchestrator 越权拒绝并记审计 | 通过 |
| R5 viewer 改阶段 403，库不变 | 通过 |
| R6 前端/语料/web-demo 无 `CURSOR_API_KEY` | 通过 |
| 不评估「像不像 GPT」、电销接通率、S9 之前的向量 Recall | 遵守 |
| `npx tsc --noEmit` | 通过 |
| `npm test` | 当时 63 passed，1 skipped（golden）；现以全量 `npm test` / eval-last-run 为准 |
| `npm run test:golden` | 1 skipped（无密钥） |
| `npm run eval:report` | 已生成 [eval-last-run.md](../eval-last-run.md) |

契约：[contracts/M5.md](../contracts/M5.md)。当时下一刀 M6。S9 已交付。
