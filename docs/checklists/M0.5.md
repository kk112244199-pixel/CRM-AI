# M0.5 检查清单（修订后）

步骤：无后端原型按企业骨架修订。

| 项 | 结果 |
|---|---|
| 对象：线索 / 客户 / 联系人 / 商机 / 活动类型 | 通过（测试） |
| 分配、建档自动生效 | 通过 |
| 转化销售确认后才拆三对象 | 通过 |
| 外发确认后才有电话活动 | 通过 |
| 销售可推进谈判；赢单须 manager | 通过 |
| 拒绝转化不建客户 | 通过 |
| 时间线展示系统分配而非销冠 | 通过（文案） |
| `npm --prefix apps/web-demo test` | 9 passed |
| `npx tsc --noEmit` | 通过 |
| 无 fetch / sdk | 通过 |
| 建档后能自动排出商机转化建议（定时器依赖含 agent） | 通过 |

请刷新后用橙果再走一遍：提交获客 → 等建档自动出现（无需确认新建）→ 再等商机转化建议出现在右侧 → 确认转化 → 确认发出。再到杭齿点建议赢单，切 manager 确认。

2026-08-16 补丁：同一线索连续排队时，前端必须用 `leadId:agent` 作为定时器依赖，否则建档之后转化建议不会出现。
