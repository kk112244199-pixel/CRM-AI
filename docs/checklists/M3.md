# M3 检查清单（S7 真控制台）

步骤：新建 `apps/web` 接 M2 HTTP。未改 `apps/web-demo`（仍无 fetch）。未用 `/frontend-design`。

| 项 | 结果 |
|---|---|
| 先读 M2 / M0.5 契约 | 通过 |
| 五屏：列表、详情（时间线主）、待确认、用量、录入 | 通过 |
| 展示名：系统分配 / 建档 / 商机 / 跟进 | 通过 |
| 橙果：ingest → 确认转化 → 确认发出，电话生效 | 通过（HTTP 测试） |
| 杭齿建议赢单，sales 确认 403，manager 通过 | 通过 |
| viewer 改阶段 403，库不变（R5） | 通过 |
| 前端包无 `CURSOR_API_KEY`、无 `@cursor/sdk`（R6） | 通过 |
| Token 顶栏走 `GET /api/usage`，合计字段可核对 | 通过 |
| 角色 `x-hengce-role` / localStorage，不暴露 Agent 端口 | 通过 |
| web-demo 未加 fetch | 通过（既有约束测试） |
| `npx tsc --noEmit` | 通过 |
| `npm test` | 50 passed，1 skipped（golden） |
| `npm --prefix apps/web test` | 3 passed |
| `npm --prefix apps/web run build` | 通过 |

契约：[contracts/M3.md](../contracts/M3.md)。当时下一刀 M4。好看已在 M6 交付。S9 已交付。
