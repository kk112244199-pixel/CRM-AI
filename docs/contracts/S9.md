# 契约 S9（SQLite 向量相似）

后续步骤把本文与 [M0.md](./M0.md)、[M0.5.md](./M0.5.md)、[M1.md](./M1.md)、[M2.md](./M2.md)、[M3.md](./M3.md)、[M4.md](./M4.md)、[M5.md](./M5.md)、[M6.md](./M6.md) 一起当作硬约束。要改先改 PRD 并问人。

## 已交付

- `searchSimilar` 走 **sqlite-vec** 虚拟表 `vec_leads`（`embedding MATCH` + L2），不再按 `search_tags` 重叠计数。
- 向量由线索档案（公司名 + 行业 + 标签）嵌入；语料全文仍只给 Q8 引用。
- `documents` 表仍无 `embedding` 列。不上 Pinecone / Milvus / Redis / GraphRAG。
- Recall@1 锁：杭齿精密机电 → 嘉兴精工装备；邻里鲜超市 → 夜灯便利；海图进出口第一名不得是杭齿精密机电。
- **未**改漏斗顺序、HITL 闸门、8 家名字、`apps/web-demo`。

打开：`npm test`（含 Q6）；控制台「对照相似」仍打 `GET /api/similar?company=`。

## 实现要点

- 开库即 `sqlite-vec.load`（`packages/domain/src/db.ts`）。
- `rebuildLeadVec` 在 seed / API 启动 / 每次检索前重建 8 行。
- HTTP 路径与权限不变。前端不装 sqlite-vec。

## 禁做

- 独立向量库、GraphRAG、Redis、MQ
- 给 `apps/web-demo` 加 `fetch`
- 改四 Agent 顺序或闸门
- 用 `/orchestrate` 调度销售 Agent
- 把相似检索改回纯 SQL 标签重叠

## 下一刀

**S10**（可选）：`/orchestrate` 加筛选与 CSV。前置：git 远程 + `CURSOR_API_KEY`。无远程则不要扇出云端工人。
