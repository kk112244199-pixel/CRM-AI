# 契约 M0.5（修订后：S7 与 M1 必须遵守）

本步锁定**交互**。视觉已在 M6 交付（只改 `apps/web`）。不要给 demo 做视觉、不要给 demo 加 `fetch`。

## 已交付

- `apps/web-demo/` 纯前端。`npm --prefix apps/web-demo run dev`
- 状态机：`apps/web-demo/src/mock/machine.ts`
- 禁止在本目录加 `fetch`、后端、`@cursor/sdk`

## 交互

1. 时间线主入口。展示名：系统分配 / 建档 / 商机 / 跟进（内部 id 仍为四个 Agent）。
2. 黄金切片（橙果）：分配自动 → 建档自动 → **确认转化** 才出现客户+联系人+商机（需求确认）→ **确认发出** 才有电话活动。系统分配 / 建档 / 商机 / 跟进四类交卷都要出现。
3. 销售可推进需求确认/方案报价/谈判，无需主管。
4. 建议赢单/丢单生成待确认，仅 manager 能过；销售点确认无效。
5. 拒绝转化：不建三张对象，不跟进。
6. 中止只清排队，历史 handoff 保留。
7. Token 合计 = 各条之和。无充值。
8. 相似：本步 demo 写死杭齿精密机电 → 嘉兴精工装备。邻里鲜超市 → 夜灯便利 在 M1 `searchSimilar` 锁住。

## 下一步

认可后 **M1** 已按本对象模型建 SQLite，**M2** 已接顺序 harness，**M3** 已接真控制台，**M6** 已做视觉，**S9** 已换 sqlite-vec。不要把 web-demo 接 API。当前硬上下文 [S9.md](./S9.md)。
