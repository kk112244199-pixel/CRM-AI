# 契约 M5（eval 报告）

后续步骤把本文与 [M0.md](./M0.md)、[M0.5.md](./M0.5.md)、[M1.md](./M1.md)、[M2.md](./M2.md)、[M3.md](./M3.md)、[M4.md](./M4.md) 一起当作硬约束。要改先改 PRD 并问人。

## 已交付

- 按 [eval.md](../eval.md) 对照 Q1–Q8、R1–R6
- `npm run eval:report` 跑 vitest JSON，写出 [eval-last-run.md](../eval-last-run.md)；**禁止手写改结论**
- Q8：引用必须能指到 `data/corpus` 文件名（`packages/harness/src/citations.ts`）
- R1：`ingestLead` 跳过已生效步骤；pipeline 前崩溃后续跑不丢 handoff、不复用同一 `run_id`
- 无密钥时 `test:golden` / Q1-golden **只许跳过**，不得记成通过

命令：

```text
npm test
npm run test:golden    # 无 DASHSCOPE_API_KEY / CURSOR_API_KEY 则跳过
npm run eval:report    # 生成 docs/eval-last-run.md
```

未通过项在报告里必须带：用例 ID、期望、实际、相关 `run_id`（没有则写 n/a）。

## 结论以生成物为准

最近一次结果见 [eval-last-run.md](../eval-last-run.md)。本契约不复述「全部通过」。无密钥环境允许 Q1 真模型跳过；脚本切片仍须通过。

## 不评估

不评估「像不像 GPT」。不评估电销接通率。向量 Recall@1 由 Q6 覆盖。

## 禁做（S9 仍禁止）

- 把手写 eval 报告当成通过
- 无密钥把黄金切片记成通过
- 给 `apps/web-demo` 加 `fetch` / `@cursor/sdk`
- 改四 Agent 顺序、改闸门、改 8 家名字
- Redis / MQ / 独立 Grafana / GraphRAG / 充值表 / `cloud: { repos }` / K8s
- 用 `/orchestrate` 调度销售 Agent

## 下一刀

**M6、S9 已交付**，见 [S9.md](./S9.md)。可选下一刀 **S10**。
