# 契约 M6（控制台视觉）

后续步骤把本文与 [M0.md](./M0.md)、[M0.5.md](./M0.5.md)、[M1.md](./M1.md)、[M2.md](./M2.md)、[M3.md](./M3.md)、[M4.md](./M4.md)、[M5.md](./M5.md) 一起当作硬约束。要改先改 PRD 并问人。

## 已交付

- 只改 `apps/web`。主画面是**四站漏斗轨**：系统分配 → 建档 → 转化 → 跟进。
- 待确认用印泥章标出。用量页是交卷台账小票，不是图表看板。
- 国内销售用语：交卷、漏斗、确认转化、确认发出、建议赢单。
- **未**改调度顺序、HITL 闸门、8 家名字、`apps/web-demo`。

打开：`npm --prefix apps/api start` 与 `npm --prefix apps/web run dev`。默认进线索详情（橙果漏斗轨）。

## 视觉（本步锁定）

- 地坪青灰 + 热敏纸，不是奶油底衬线店、不是黑底霓虹、不是紫渐变 dashboard。
- 铭牌用宋体（公司名 / 站名），正文用黑体，token 用等宽。
- 签名元素：铝轨四站 + 待确认印泥章。不要再叠一套装饰。

闸门与 M0.5 / M3 相同：分配/建档自动；转化与外发销售确认；赢单/丢单仅经理。

## 禁做

- 改四 Agent 顺序或闸门
- 给 `apps/web-demo` 加 `fetch`
- Redis / MQ / Grafana / GraphRAG / 充值 / `cloud: { repos }`
- 用 `/orchestrate` 调度销售 Agent

## 下一刀

**S9 已交付**，见 [S9.md](./S9.md)。可选 **S10** 筛选/CSV（需 git 远程）。
